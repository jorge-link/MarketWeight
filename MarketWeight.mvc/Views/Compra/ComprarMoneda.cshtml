@model MarketWeight.mvc.ViewModels.VMComprarMoneda

@{
    ViewData["Title"] = "Comprar Moneda";
}

<h2>Comprar Moneda</h2>

<form asp-action="ComprarMoneda" asp-controller="Compra" method="post">
    <div class="form-group">
        <label for="IdMoneda">Selecciona una moneda</label>
        <select asp-for="IdMoneda" asp-items="Model.ListaMoneda" class="form-control"></select>
    </div>

    <div class="form-group mt-3">
        <label for="Cantidad">Cantidad</label>
        <input type="number" step="0.00000001" name="Cantidad" class="form-control" required />
    </div>

    <input type="hidden" name="IdUsuario" value="@ViewBag.IdUsuario" />

    <button type="submit" class="btn btn-primary mt-3">Comprar</button>
</form>
